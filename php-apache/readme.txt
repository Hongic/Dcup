log		日记目录

vhost		apache虚拟主机配置

Dockerfile	镜像配置文件

http.conf	apache配置

php.ini		php配置文件


注意：php:7.1-apache镜像的配置

镜像默认的php配置文件路径   	  /usr/local/etc/php/conf.d/

镜像默认的apache配置文件路径	  /etc/apache2/
镜像默认的apache虚拟主机文件路径  /etc/apache2/sites-enabled/
镜像默认的apache项目目录          /var/www/html/

