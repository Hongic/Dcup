log		日记目录

vhost		nginx虚拟主机配置

Dockerfile	镜像配置文件

nginx.conf	nginx配置


注意：nginx:alpine镜像的配置

镜像默认的nginx配置文件路径 		/etc/nginx/nginx.conf
镜像默认的nginx虚拟主机文件路径  	/etc/nginx/conf.d/
镜像默认的项目路径  			/usr/share/nginx/html